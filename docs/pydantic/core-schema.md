# Core Schema

## 왜 중요한가

Pydantic v2를 이해하려면 겉의 `BaseModel`보다 안쪽의 core schema 감각이 먼저 필요하다.

## 이 장에서 채울 것

- pydantic-core가 하는 일
- schema graph 개념
- validation rule이 schema로 컴파일되는 감각
- custom type가 schema에 끼어드는 방식
