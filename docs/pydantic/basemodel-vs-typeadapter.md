# BaseModel vs TypeAdapter

## 왜 중요한가

Pydantic v2에서는 모든 것을 `BaseModel`로 풀 필요가 없다. `TypeAdapter`를 이해하면 더 가볍고 정확한 설계가 가능하다.

## 이 장에서 채울 것

- BaseModel이 필요한 경우
- TypeAdapter가 더 나은 경우
- validation only path
- nested typed structure 처리
