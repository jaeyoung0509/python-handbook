# Dependency Injection

## Why It Matters

FastAPI's DI system is convenient, but it can also blur the line between explicit wiring and hidden global state if used carelessly.

## This Chapter Builds

- `Depends`
- the dependency graph
- yield dependencies
- request-scoped resources
- test overrides

## Practical Connections

- database sessions
- auth principal injection
- external client lifecycle management
