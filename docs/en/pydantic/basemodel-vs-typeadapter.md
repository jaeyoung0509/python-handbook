# BaseModel vs TypeAdapter

## Why It Matters

`BaseModel` is not the only entry point in Pydantic v2. Sometimes `TypeAdapter` is the cleaner and lighter tool.

## This Chapter Builds

- when you need `BaseModel`
- when `TypeAdapter` is better
- validation-only paths
- nested typed structures
