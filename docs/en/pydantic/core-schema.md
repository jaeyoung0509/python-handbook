# Core Schema

## Why It Matters

To understand Pydantic v2, you need the mental model of core schema before you focus on `BaseModel` ergonomics.

## This Chapter Builds

- what `pydantic-core` does
- the idea of a schema graph
- how validation rules compile into schema
- how custom types plug into the schema layer
