# FastAPI + Pydantic + SQLAlchemy

## Goal

Show how to combine request models, domain logic, sessions, and serialization without collapsing everything into one layer.

## This Chapter Builds

- request DTO, domain model, and ORM model separation
- session boundaries
- transaction management
- serialization boundaries
- async integration
